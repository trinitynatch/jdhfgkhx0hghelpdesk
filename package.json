{
  "name": "redirect-service",
  "version": "1.0.0",
  "description": "Encrypted, allow-listed URL redirect service using AES-256-GCM",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --watch --env-file=.env server.js"
  },
  "engines": {
    "node": ">=20.6.0"
  },
  "keywords": [
    "redirect",
    "url-shortener",
    "click-tracking",
    "encryption"
  ],
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.19.2"
  }
}
